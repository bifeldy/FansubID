export interface DialogInfoData {
  title: string;
  htmlMessage: string;
  confirmText: string;
  cancelText?: string;
}

export interface DialogDmakData {
  romaji: string;
  hiragana_katakana_kanji: string;
}
